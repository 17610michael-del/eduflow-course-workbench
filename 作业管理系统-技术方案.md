# 师生作业互动系统 — 技术方案（供 GPT 编程使用）

> 目标：一个类似国外学校作业系统（如 Canvas / Moodle / Google Classroom）的网页应用。
> 老师可以布置任务，学生可以在每个任务下与老师互动、提问；
> 预留"学生学业背景表格"上传入口和"AI 分析 API"接口，用于后续分析每个学生的实际掌握情况。

---

## 1. 整体布局（完全对标 GitLab Dashboard / Work Item 界面）

> 参考截图：git.moilab.net 的 GitLab 界面。核心思路：**老师布置的每个任务 = 一个 GitLab Work Item（Issue）**，学生在任务下方的 Activity 评论区提问互动。

### 1.1 首页（对标 GitLab Home 页）

```
┌──────────────────────────────────────────────────────────┐
│ 顶部导航栏: Logo │ Search or go to... │ 🔔 │ 用户头像      │
├────────┬─────────────────────────────────────────────────┤
│ 侧边栏  │  👤 老师姓名                        Can't take.. │
│ (浅灰紫) │                                                  │
│ Your wk │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌────────┐ │
│ 🏠 Home │  │ 待我审阅 0│ │ 指派给我 1│ │ 我发布 0 │ │Quick   │ │
│ 📋 任务 │  └─────────┘ └─────────┘ └─────────┘ │access │ │
│ 👥 学生 │                                            └────────┘ │
│ 📈 学情 │  Items that need your attention          ─────────── │
│   分析 │  ▸ 学生B 评论了「第2次作业」..."老师请问数据..."         │
│        │                                                  │
│        │  Follow the latest updates            ─────────── │
│        │  ▸ 示例教师在任务 #1 中回复：已上传附件，请查收        │
└────────┴─────────────────────────────────────────────────┘
```

### 1.2 任务详情页（核心页面，对标 GitLab Work Item / Issue 页）

```
┌──────────────────────────────────────────────────────────┐
│ 侧边栏        │ 任务标题：第2次作业 - 文献综述               │
│              │ 🟢Open  Task created 2 weeks ago by 老师   │──┬──────────────┐
│ Project     │─────────────────────────────────────────────  │ Assignee     │
│ 📌 Pinned   │  ## 任务要求（Markdown 渲染正文）              │  👤 全体学生  │
│ 📋 任务列表  │  1. 阅读指定文献 3 篇                          │ Labels       │
│ 👥 学生名单  │  2. 完成 800 字综述                            │ 🏷 第2次作业  │
│ 📈 学情分析  │  3. 截止前在下方提交 PDF                       │ Milestone    │
│              │                                              │ Dates        │
│              │  ### 任务分配                                  │ Due: 8月20日 │
│              │  @张三 @李四 @王五                            │──────────────│
│              │  请各位在截止日期前完成并提交。                  │ Time tracking│
│              │                                              │ 3 Participants│
│              │──────────────────────────────────────────────┴──────────────┘
│              │  ## Activity（互动区 = 师生问答）                All activity ▾│
│              │  ┌─────────────────────────────────────────────┐│
│              │  │ 🧑‍🎓 张三  2 days ago                          ││
│              │  │ 老师，第3篇文献的访问链接失效了，能补一份吗？   ││
│              │  │                                             ││
│              │  │ 🧑‍🏫 老师  1 day ago                           ││
│              │  │ @张三 已重新上传到附件区，请查收。             ││
│              │  └─────────────────────────────────────────────┘│
│              │  ┌─────────────────────────────────────────────┐│
│              │  │ [B I 🔗 💻 富文本工具栏]                      ││
│              │  │ Write a comment or drag your files here...  ││
│              │  │                       [Comment] [Close task]││
│              │  └─────────────────────────────────────────────┘│
└──────────────┴───────────────────────────────────────────────┘
```

- **侧边栏菜单**（浅灰紫底色，GitLab 风格）：Home / 任务列表 / 学生名单 / 学情分析
- **任务正文**：Markdown 渲染，含"任务分配"小节 @相关学生
- **右侧边栏**：Assignee（指派学生）、Labels（如"第2次作业"）、Due date、Participants
- **Activity 评论区**：师生问答主战场，富文本编辑器，支持拖拽文件上传

---

## 2. 用户注册与角色

| 角色 | 权限 |
|---|---|
| 老师（Teacher） | 创建/编辑/删除任务、上传学生背景表、查看分析结果、回复学生提问、审核学生注册 |
| 学生（Student） | 查看任务、提交作业、在任务下提问/互动、查看老师回复 |

### 2.1 注册登录方案（部署到公网必需）

**注册流程（学生）**

1. 注册页字段：学号、姓名、邮箱（或手机号）、密码、确认密码
2. 密码要求：≥8 位，含字母+数字；服务端用 **bcrypt / werkzeug `generate_password_hash`** 加盐哈希存储，**绝不存明文**
3. 提交后状态为 `pending`（待审核），**老师在"学生名单"页一键批准**后账号激活——这样避免陌生人注册进班
4. 可选简化版：老师提前在后台批量导入学号白名单，学生注册时学号必须在白名单内才允许注册，注册即激活（免审核）

**老师账号**：不开放注册，首次部署时通过命令行/种子脚本创建（`flask create-teacher` 或初始化 SQL）

**登录 / 会话**

- 登录页：学号（或邮箱）+ 密码
- 会话方案（二选一）：
  - **Flask-Login + Session Cookie**（推荐，Flask 方案最省事）
  - JWT（前后端分离时选这个，token 存 localStorage）
- 登录失败 5 次锁定 15 分钟（简单防爆破）
- 所有 `/api/*` 接口除登录/注册外都要求登录态；老师专属接口用装饰器 `@teacher_required` 校验角色

**users 表更新为**：

```sql
users(
  id, student_no UNIQUE,     -- 学号（老师可为 NULL 或用工号）
  name, email UNIQUE,
  password_hash,             -- bcrypt 哈希
  role ENUM('teacher','student'),
  status ENUM('pending','active','disabled'),  -- 注册审核状态
  created_at, last_login_at
)
```

---

## 3. 核心功能模块

### 3.1 任务管理（老师端）
- 老师创建任务：标题、说明（富文本/Markdown）、截止日期、附件（可选）
- 任务列表：按截止时间排序，显示状态（进行中 / 已截止）
- 任务卡片上显示统计：已提交 x/y 人、未读提问数

### 3.2 任务详情页 + 互动区（核心）
任务详情页完全对标 **GitLab Work Item（Issue）页**（见 1.2 节线框），由四部分组成：

1. **头部**：任务标题 + 状态徽章（🟢Open / ⚪Closed）+ "created xx ago by 老师"
2. **正文**：Markdown 渲染的任务说明，内含「任务分配」小节 @相关学生（例如“任务分配 @student01 @student02”）
3. **右侧边栏**：Assignee（指派的学生）、Labels（如"第2次作业"，GitLab 紫色标签风格）、Due date、Participants 头像列表
4. **Activity 互动区**（对标 GitLab Issue 评论区）：
   - 按时间流显示：系统事件（"老师 添加了 label"、"老师 指派给 @张三"）+ 评论混排
   - 评论支持**两层回复**：主帖（学生提问）+ 回复（老师/其他同学）
   - 底部是富文本编辑器（B / I / 链接 / 代码 / 列表 / 图片工具栏），占位文字 "Write a comment or drag your files here..."
   - 支持拖拽文件到评论框上传（学生可用此方式提交作业文件）
   - `[Comment]` 和 `[Close task]` 按钮（Close 仅老师可见）
   - 新评论时首页"Items that need your attention"出现提醒条目

### 3.3 学生学业背景表格（预留模块）
- "学生名单"页面：展示一张表格，字段预留如下：

| 学号 | 姓名 | 专业 | 年级 | 前置课程成绩 | 编程基础 | 数学基础 | 英语水平 | 备注 |
|---|---|---|---|---|---|---|---|---|

- 支持 **CSV / Excel 上传**导入学生背景数据（第一版可先支持 CSV）
- 表格可手动编辑、添加行
- 数据存入 `students` 表

### 3.4 学情分析 API（预留模块）
- "学情分析"页面：调用后端 `/api/analyze` 接口
- **第一版用 Mock/占位实现**：接口接收学生背景数据 + 该生在各任务下的提问记录、提交情况，返回一个固定格式的分析结果（占位数据）
- 分析结果示例格式（JSON）：

```json
{
  "student_id": "2024001",
  "name": "张三",
  "mastery": {
    "overall_score": 0.72,
    "weak_topics": ["数据结构", "递归"],
    "engagement": "active",
    "suggestion": "建议补充递归相关练习"
  },
  "generated_at": "2026-08-10T20:00:00"
}
```

- 后续真正接入 AI 分析时，只需替换 `/api/analyze` 的内部实现（例如把数据发给 LLM），**接口签名保持不变**

---

## 4. 数据模型（数据库表）

```sql
-- 用户（老师和学生共用，role 区分；含注册审核状态）
users(
  id, student_no UNIQUE, email UNIQUE,
  name, password_hash,      -- bcrypt，绝不存明文
  role ENUM('teacher','student'),
  status ENUM('pending','active','disabled'),
  created_at, last_login_at
)

-- 学生学业背景（预留表格）
students(
  id, student_no, name, major, grade,
  prereq_scores JSON,      -- 前置课程成绩
  programming_level,       -- 编程基础: 无/入门/熟练
  math_level,              -- 数学基础
  english_level,           -- 英语水平
  notes, created_at, updated_at
)

-- 任务
assignments(
  id, title, description,   -- Markdown
  due_date, attachment_url,
  created_by -> users.id, created_at, status
)

-- 提问/讨论（自引用实现两层回复）
discussions(
  id, assignment_id -> assignments.id,
  user_id -> users.id,
  parent_id -> discussions.id NULL,  -- NULL = 主帖，否则 = 回复
  content, created_at
)

-- 作业提交
submissions(
  id, assignment_id, student_id,
  file_url, submitted_at, version
)

-- 学情分析结果（缓存每次分析结果）
analysis_reports(
  id, student_id, report JSON, generated_at
)
```

---

## 5. API 设计（RESTful）

| 方法 | 路径 | 说明 | 权限 |
|---|---|---|---|
| POST | `/api/auth/register` | 学生注册（进入 pending 待审核） | 公开 |
| POST | `/api/auth/login` | 登录（学号/邮箱 + 密码） | 公开 |
| POST | `/api/auth/logout` | 退出登录 | 登录 |
| GET | `/api/auth/me` | 当前用户信息 | 登录 |
| GET | `/api/auth/pending` | 待审核注册列表 | 老师 |
| POST | `/api/auth/approve/:id` | 批准/拒绝学生注册 | 老师 |
| GET | `/api/assignments` | 任务列表 | 登录 |
| POST | `/api/assignments` | 老师创建任务 | 老师 |
| GET | `/api/assignments/:id` | 任务详情 | 登录 |
| GET | `/api/assignments/:id/discussions` | 某任务下的讨论串 | 登录 |
| POST | `/api/assignments/:id/discussions` | 发帖/回复（body 带 parent_id 可选） | 登录 |
| POST | `/api/assignments/:id/submissions` | 学生提交作业（文件上传） | 学生 |
| GET | `/api/students` | 学生背景表 | 老师 |
| POST | `/api/students/import` | CSV 导入学生背景（预留） | 老师 |
| PUT | `/api/students/:id` | 编辑学生背景 | 老师 |
| **POST** | **`/api/analyze`** | **学情分析（预留，先返回 Mock 数据）** | 老师 |

`/api/analyze` 请求体：

```json
{ "student_id": "2024001" }
```

服务端内部逻辑（伪代码）：

```python
def analyze(student_id):
    student = get_student(student_id)           # 背景表数据
    activity = get_activity(student_id)          # 提问数、提交情况
    # TODO: 这里接入真正的 AI 分析（调用 LLM API）
    return mock_analysis(student, activity)      # 第一版返回占位结果
```

---

## 6. 技术栈建议（二选一）

**方案 A：简单快速（推荐给 GPT 一把梭）**
- 前端：HTML + Tailwind CSS + 少量原生 JS（单页即可）
- 后端：Python Flask（或 Node.js Express）
- 数据库：SQLite（零配置，一个文件）
- 文件上传：本地 `uploads/` 目录

**方案 B：现代化工程**
- 前端：React + Vite + Tailwind CSS + shadcn/ui
- 后端：FastAPI (Python) 或 Express (Node.js)
- 数据库：SQLite → 后期可换 PostgreSQL

> 如果是演示/课程项目，**强烈建议方案 A**，一个 Flask 文件 + 几个 HTML 模板就能跑起来。

---

## 7. 页面清单（开发任务分解）

1. `/register` 注册页：学号 / 姓名 / 邮箱 / 密码 / 确认密码，提交后提示"等待老师审核"
2. `/login` 登录页：学号（或邮箱）+ 密码，未登录访问任何页面自动跳转这里
3. `layout`：顶部导航（Logo + 搜索框 + 通知 + 头像，头像下拉显示当前用户和退出登录）+ 左侧浅灰紫侧边栏（GitLab 风格）
4. `/` 首页（对标 GitLab Home）：统计卡片（待我审阅 / 指派给我 / 我发布的）+ "Items that need your attention" + "Follow the latest updates" 动态流
5. `/assignments` 任务列表页（对标 GitLab Projects/Work items 列表）：搜索框 + 按创建时间排序，每行显示任务标题、label、评论数、创建时间
6. `/assignments/:id` 任务详情页（核心，对标 GitLab Work Item）：Markdown 正文 + 右侧 Assignee/Labels/Dates/Participants 边栏 + Activity 评论区 + 富文本编辑器
7. `/assignments/new`：老师创建任务表单（标题、Markdown 正文、截止日期、指派学生、labels、附件）
8. `/students`：学生背景表格（可编辑 + CSV 上传按钮）+ **待审核注册列表**（批准/拒绝按钮）
9. `/analytics`：学情分析页（选学生 → 点"分析" → 展示返回的 JSON 美化后的卡片）

---

## 8. 阿里云部署方案（交给 Codex 执行）

### 8.1 架构总览

```
学生/老师浏览器
      │ HTTPS (443)
      ▼
┌─────────────────────────────────┐
│ 阿里云 ECS（1台即可起步）          │
│  ┌───────────────────────────┐  │
│  │ Nginx                     │  │  ← 反向代理 + HTTPS 终止 + 静态文件
│  │   ├─ /          → gunicorn│  │
│  │   └─ /uploads/  → 直接发文件│  │
│  └───────────┬───────────────┘  │
│              ▼                   │
│  Gunicorn (4 workers) :8000      │  ← WSGI 服务
│              │                   │
│              ▼                   │
│  Flask 应用                      │
│    ├─ SQLite 文件 (/data/app.db) │
│    └─ uploads/  (/data/uploads)  │
└─────────────────────────────────┘
  systemd 托管 gunicorn 开机自启 + 崩溃自动重启
```

### 8.2 采购清单

| 项 | 建议 | 备注 |
|---|---|---|
| ECS 实例 | 2 核 2G / 2 核 4G，Ubuntu 22.04 LTS | 一个班几十人，2核2G 足够 |
| 带宽 | 按流量计费，峰值 5Mbps | 学生传 PDF 够用，省钱 |
| 系统盘 | 40G ESSD | |
| 域名 | 阿里云万网买一个（如 `class-xxxx.com`） | **国内服务器需 ICP 备案**，约 2-3 周；急用可先 IP+端口访问 |
| HTTPS 证书 | 阿里云免费 SSL 证书（DigiCert，1 年期） | 备案后绑定域名申请 |
| 安全组 | 只放行 22 (SSH, 限自己 IP)、80、443 | 8000 端口不对外，只走 Nginx |

### 8.3 部署步骤（Codex 要执行的）

```bash
# 1. 系统准备
sudo apt update && sudo apt install -y python3 python3-venv nginx git

# 2. 拉代码 + 建虚拟环境
cd /opt && git clone <repo> homework && cd homework
python3 -m venv venv && source venv/bin/activate
pip install -r requirements.txt   # flask gunicorn flask-login werkzeug ...

# 3. 初始化（建库 + 创建老师账号）
mkdir -p /data/uploads
flask init-db
flask create-teacher --email teacher@example.com   # 交互式设密码

# 4. systemd 服务 /etc/systemd/system/homework.service
#    ExecStart=/opt/homework/venv/bin/gunicorn -w 4 -b 127.0.0.1:8000 app:app
#    Environment=SECRET_KEY=<随机串> DATABASE=/data/app.db UPLOAD_DIR=/data/uploads
sudo systemctl enable --now homework

# 5. Nginx 站点配置（反代到 127.0.0.1:8000，client_max_body_size 20m）
#    证书申请后用 certbot 或阿里云证书配置 443
sudo nginx -t && sudo systemctl reload nginx

# 6. 定时备份（crontab，每天凌晨 3 点备份数据库和上传文件）
0 3 * * * tar czf /data/backup/$(date +\%F).tar.gz /data/app.db /data/uploads
```

### 8.4 安全要点（写进 Codex 提示词）

- `SECRET_KEY` 等敏感配置走**环境变量**，不硬编码进代码
- 密码 bcrypt 哈希；登录失败 5 次锁 15 分钟
- 上传文件校验扩展名（pdf/docx/zip 等白名单）+ 大小限制 20MB，文件名重命名为 UUID 防路径穿越
- Nginx 只暴露 80/443；Flask 监听 127.0.0.1 不对外
- 无域名备案期间，可先用 `http://<ECS公网IP>` 临时访问测试，正式上线务必上 HTTPS

---

## 9. 可以直接发给 Codex 的提示词（Prompt）

````
请用 Python Flask + SQLite + Tailwind CSS 开发一个师生作业互动系统，并完成阿里云 ECS（Ubuntu 22.04）部署。界面完全模仿 GitLab（GitLab Community Edition 的 Dashboard 和 Issue/Work Item 页面风格）。

【功能要求】
1. 用户系统：学生注册（学号/姓名/邮箱/密码，注册后 pending 待老师审核）、登录/退出（Flask-Login + Session），密码用 werkzeug generate_password_hash 哈希存储，登录失败 5 次锁定 15 分钟。老师账号不开放注册，提供 `flask create-teacher` 命令创建。除登录/注册外所有页面和 API 都要求登录，老师专属操作用 @teacher_required 装饰器控制。

2. 布局：顶部导航栏（Logo + "Search or go to..." 搜索框 + 通知图标 + 用户头像，头像下拉显示用户名和退出登录）；左侧浅灰紫（#f5f3f7）侧边栏，菜单：Home / 任务列表 / 学生名单 / 学情分析。

3. 首页模仿 GitLab Home：统计卡片（待我审阅 / 指派给我 / 我发布的）+ "Items that need your attention"（学生新提问提醒）+ "Follow the latest updates" 动态流。

4. 任务列表页模仿 GitLab Work items 列表：搜索框 + 排序，每行显示标题、紫色 label、评论数、创建时间。

5. 任务详情页（核心）模仿 GitLab Issue 页：
   - 头部：标题 + 绿色 Open 徽章 + "Task created xx ago by 老师"
   - 正文：Markdown 渲染任务说明，含"任务分配"小节 @学生
   - 右侧边栏：Assignee / Labels（紫色）/ Dates（Due date）/ Participants
   - Activity 区：时间流混排系统事件（"老师 添加了 label"等）和评论，评论支持两层回复（学生提问 → 老师回复）
   - 底部富文本编辑器（B/I/链接/代码/列表工具栏），占位文字 "Write a comment or drag your files here..."，支持拖拽文件上传（学生以此交作业），按钮 [Comment] 和 [Close task]（Close 仅老师可见）

6. 学生名单页：学业背景表格（学号/姓名/专业/年级/前置课程成绩/编程基础/数学基础/英语水平/备注），支持编辑和 CSV 导入；页面顶部有"待审核注册"列表，老师可批准/拒绝。

7. 学情分析页：POST /api/analyze 接口，输入 student_id 返回 JSON（overall_score、weak_topics、engagement、suggestion 字段），先 Mock 占位并留 TODO 注释方便以后接 LLM；页面下拉选学生，点"开始分析"渲染成美观卡片。

8. 数据库 SQLite，表：users(id, student_no, email, name, password_hash, role, status, created_at, last_login_at) / students / assignments / discussions(parent_id 自引用) / submissions / analysis_reports。

9. 种子数据：1 老师、3 学生、2 任务（含完整正文 + 任务分配 @学生示例）、4-5 条评论（含系统事件）。

10. 安全：SECRET_KEY 等走环境变量；上传文件扩展名白名单（pdf/docx/zip/png/jpg）+ 20MB 限制 + UUID 重命名；Markdown 渲染防 XSS（用 bleach 或 markupsafe 过滤）。

【部署要求】（阿里云 ECS，Ubuntu 22.04）
- 提供 requirements.txt、README 部署文档
- Gunicorn 4 workers 监听 127.0.0.1:8000，提供 systemd service 文件（开机自启、崩溃重启）
- 提供 Nginx 站点配置：80 反代到 8000、/uploads/ 直接由 Nginx 发文件、client_max_body_size 20m、443 HTTPS 配置段（证书路径留占位注释）
- SQLite 数据库和 uploads/ 放在 /data/ 目录，提供每日备份 crontab 命令
- 提供一键部署脚本 deploy.sh 或 Makefile

请给出完整可运行代码、配置文件和部署文档。
````

---

## 10. 后续扩展路线

| 阶段 | 内容 |
|---|---|
| V1（本次） | 注册登录 + 任务 + 讨论 + 背景表 + Mock 分析 + 阿里云部署 |
| V2 | 接入真实 LLM 分析：把背景表 + 提问记录 + 提交情况拼成 prompt 发给大模型，生成个性化掌握情况报告 |
| V3 | 邮箱验证、站内/邮件通知、截止提醒、忘记密码找回 |
| V4 | 老师批改界面、评分、成绩曲线图 |
